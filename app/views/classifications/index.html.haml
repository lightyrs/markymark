- content_for :list_menu do

  %h3 Classifications

- content_for :main do

  = default_form_for StructuralClassification.new, url: classifications_url do |f|
    
    = f.input :name, as: :grouped_collection_select, collection: Classification.all.group_by(&:content_type), group_method: :last, label: false, input_html: { class: 'select2 input-lg', 'data-placeholder' => 'Classify the Document Structure' }
